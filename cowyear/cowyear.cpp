#include <iostream>
#include <map>
#include <string>
using namespace std;

int main() {
    int n; cin >> n;
    int i = n;
    map<string, int> born;
    born["Bessie"] = 120;
    while (i--) {
        string cow1;
        cin >> cow1;
        string bruh;
        cin >> bruh >> bruh;
        int year;
        cin >> year;
        cin >> bruh;
        string when;
        cin >> when;
        string cow2;
        cin >> cow2;
    }
}